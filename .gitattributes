# Normalize line endings so the repo is byte-identical on every machine and the
# .toc the client reads is always LF, whatever a contributor's editor does.
* text=auto eol=lf

# Binary assets: never touch their bytes.
*.png binary
*.jpg binary
*.gif binary
